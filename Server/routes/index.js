const auth_route = require('./auth');
const user_route = require('./user.js');
const book_route = require('./book.js');
const shelf_route = require('./shelf.js');

module.exports = {
    auth_route,
    user_route,
    book_route,
    shelf_route,
};